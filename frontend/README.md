Tamil News Frontend

Setup

1. Copy .env.example to .env and adjust VITE_API_BASE_URL if needed.
2. Install deps: npm install
3. Run dev server: npm run dev
4. Build: npm run build, Preview: npm run preview

Notes

- Backend endpoints used: GET /news/?limit=, POST /admin/fetch
- CORS must allow http://localhost:5173
